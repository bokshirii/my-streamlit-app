import streamlit as st

# -----------------------------
# 기본 설정
# -----------------------------
st.set_page_config(page_title="🧩 오늘의 첫 행동 만들기", layout="centered")


# -----------------------------
# 핵심 로직
# -----------------------------

def suggest_first_action(goal: str) -> str:
    cleaned = goal.strip().strip('"').strip("'")
    return f"타이머 5분 맞추고 {cleaned}을 위해 지금 할 수 있는 가장 작은 행동 하나를 적기."


# -----------------------------
# UI
# -----------------------------

st.title("🧩 오늘의 첫 행동 만들기")
st.write("목표를 입력하면 오늘 바로 시작할 수 있는 아주 작은 첫 행동을 제시합니다.")

goal = st.text_input("목표를 입력하세요", placeholder="예: 영어 공부를 꾸준히 하고 싶다")

st.divider()

if st.button("첫 행동 만들기", type="primary"):
    if not goal.strip():
        st.error("목표를 입력해주세요.")
    else:
        st.subheader("✅ 첫 행동")
        st.write(suggest_first_action(goal))
